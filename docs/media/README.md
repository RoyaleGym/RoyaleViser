# README media

Every file the README embeds. A *placeholder* is a generated SVG whose caption says what the
real image or screen recording must show; replace the file, keep the name.

| File | Kind | Shows / must show |
|---|---|---|
| `compare-ghost.svg` | video placeholder | Two recordings, one board - the second recording drawn as hollow white ghosts on the first; on a tick where they disagree the ghost steps off the solid unit and the differ counter goes up |
| `engine-trace.svg` | video placeholder | Open an engine trace - a self-play battle recorded from the engine one frame per tick, opened with --start-tick 900, then a unit is clicked and its inspector fields change tick by tick |
| `family.svg` | diagram | the five repos and how they depend on each other; final |
| `live-training-env.svg` | video placeholder | A training run, watched live - a RoyaleGym self-play environment stepping in one terminal, python -m royaleviser --stream 127.0.0.1:9870 in another: the LIVE pill, the frame rate, a tower falls |
| `replay-scrubbed-4x.svg` | video placeholder | Replay a recorded match - a recorded battle played at 4x, paused, then stepped one frame at a time with the arrow keys and the wheel; the bar under the board seeks |
| `tile-compare.png` | real still | already final |
| `tile-event-log.png` | real still | already final |
| `tile-inspector.png` | real still | already final |
| `tile-live-stream.png` | real still | already final |
| `tile-paths-and-targets.png` | real still | already final |
| `tile-synthetic-battle.png` | real still | already final |
